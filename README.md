# my-config-files

* Instead of using the regular `pixi` command use `./pixi`

## [Just](https://just.systems/man/en) task runner
* Shell completion for `just` in `zsh`:
  * Run `just --completions zsh > $ZSH_CUSTOM/just.zsh`